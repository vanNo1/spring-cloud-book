
server.port=7070
spring.application.name=book
eureka.client.serviceUrl.defaultZone=http://localhost:8761/eureka/
eureka.instance.instance-id=${spring.application.name}:${spring.cloud.client.ip-address}:${server.port}


ribbon.ServerListRefreshInterval=3000
eureka.client.registryFetchIntervalSeconds=3

management.endpoints.web.exposure.include=*

spring.datasource.url=jdbc:mysql://47.97.180.232:3306/book3?characterEncoding=utf8&useSSL=false
spring.datasource.username=book3
spring.datasource.password=321asd
spring.datasource.driver-class-name=com.mysql.jdbc.Driver

redis1.ip=127.0.0.1
redis1.port=6379
redis.max.total=20
redis.max.idle=10
redis.min.idle=2
redis.test.borrow=true
redis.test.return=false

# springSession....................
# Session store type.
spring.session.store-type=redis
# Redis server host.
spring.redis.host=localhost
# Login password of the redis server.
#spring.redis.password=
# Redis server port.
spring.redis.port=6379
#springSessionTimeout
server.session.timeout=36000